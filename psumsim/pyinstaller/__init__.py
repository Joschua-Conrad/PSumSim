"""Hooks and entry points to work with `PyInstaller <https://pyinstaller.org/en/stable/>`_.

The hooks specify what is special about this package. See
`Understanding PyInstaller Hooks <https://pyinstaller.org/en/stable/hooks.html>`_
and `hooksample on GitHub <https://github.com/pyinstaller/hooksample>`_.

Note that a submodule *hook-psumsim* exists here, but is not shown in the
documentation, as documentation tools do not support a hyphen in module name.
But that hyphen is required by PyInstaller."""
